# Proxy API calls from the Pages domain to the Worker
# REPLACE WITH_YOUR_WORKER WITH YOUR ACTUAL WORKER SUBDOMAIN
# Example: if your worker is at portfolio-api.username.workers.dev, use:
# /api/*  https://portfolio-api.username.workers.dev/api/:splat  200
/api/*  https://WITH_YOUR_WORKER/api/:splat  200

# SPA fallback: serve index.html for unknown routes
/*  /index.html  200
